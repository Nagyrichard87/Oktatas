// Nevek
// egyértelmű elnevezések
let n
let nrOfProducts
let numberOfProducts
//Ne legyen a név félrevezető
function multiplier(x,y) {
    return x*y;
}

// Kerüld az O és I
 // let IO; let 10;

// komment két per jel, 1 soros
//this function gives roles of useer.
function getUserRoles(usser){
    return getUserRoles(user)
    
}

// matches any alphabetical caracters between m and n
let idPattern= /m[a-z]n/

// Formázás
function getUserRoles(usser){
   if(user.admin){
    return getUserRoles(user)
} else{sometnig.userObjects}
}